import { enUS as dateLocale } from 'date-fns/locale';
import { enUS as uiLocale } from '@material-ui/core/locale';
import { Locales } from './locales.interface';

const enLocale: Locales = {
  dateLocale,
  uiLocale,
};

export default enLocale;
